# MyMvcApp – ASP.NET Core MVC Application

📌 Project Overview
"MyMvcApp" is an ASP.NET Core MVC web application built using "Entity Framework Core (Code First approach)".  
The project demonstrates CRUD operations, user authentication, role-based authorization, and database integration using SQL Server.


🛠️ Technologies Used
- ASP.NET Core MVC (.NET 8)
- Entity Framework Core (Code First)
- ASP.NET Core Identity
- SQL Server / LocalDB
- Visual Studio Code
- C#


🗄️ Database Approach
This project follows the **Code First approach**.

- Database schema is generated from C# model classes
- Entity Framework Core migrations are used
- No existing database was used initially


⚙️ Prerequisites
Make sure the following are installed on your system:

- .NET 8 SDK  
  ```bash
  dotnet --version
